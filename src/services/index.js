const { loginTolken } = require('./loginService');
const userService = require('./userService');
const categoriesService = require('./categoriesService');

module.exports = {
    loginTolken,
    userService,
    categoriesService,
};